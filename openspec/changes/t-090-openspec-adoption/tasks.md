# Tasks

- [ ] Add `openspec/project.md` as the repo-local OpenSpec entry point.
- [ ] Add stable specs for `repo-governance`, `orchestration`, and `harness`.
- [ ] Create the `t-090` change package with proposal, design, tasks, and spec delta files.
- [ ] Add a dedicated OpenSpec validation script and package.json entry.
- [ ] Update `AGENTS.md` and `docs/AI_OPERATING_MODEL.md` with the OpenSpec usage boundary.
- [ ] Run static validation and the new OpenSpec validator.
